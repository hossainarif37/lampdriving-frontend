import { FC } from 'react';
import InstructorDetails from '../instructor-details/InstructorDetails';

const page: FC = () => {
    return (
        <div>
            <InstructorDetails/>
        </div>
    );
};

export default page;