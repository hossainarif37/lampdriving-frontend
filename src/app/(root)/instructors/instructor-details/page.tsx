import { FC } from 'react';
import InstructorDetails from './InstructorDetails';

const page: FC = () => {
    return (
        <div>
            <InstructorDetails/>
        </div>
    );
};

export default page;